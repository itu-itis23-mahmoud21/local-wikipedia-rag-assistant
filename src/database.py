"""SQLite metadata layer placeholder.

This module will create and manage the local SQLite database used to store
entity records, source URLs, chunk metadata, ingestion timestamps, and any other
non-vector metadata needed by the assistant.
"""


def initialize_database() -> None:
    """Initialize the SQLite schema.

    The real schema creation logic will be added in a later step.
    """

    raise NotImplementedError("SQLite initialization is not implemented yet.")
