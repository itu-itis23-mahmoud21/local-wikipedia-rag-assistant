"""Retrieval placeholder.

This module will embed user questions, query Chroma, apply metadata filters when
useful, and return the most relevant grounded context chunks.
"""


def retrieve_context(query: str) -> list[dict[str, str]]:
    """Retrieve relevant chunks for a user query."""

    raise NotImplementedError("Retrieval is not implemented yet.")
